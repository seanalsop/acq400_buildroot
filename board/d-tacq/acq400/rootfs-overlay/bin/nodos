#!/bin/sh
# nodos ... remove DOS LFCR combinations

for file in $*
do
	if [ -f $file ]; then
		echo nodos ... $file
		cp $file $file.dos
		tr -d '\015' <$file.dos >$file
		shift
	else
		exit;
	fi
done

